project_id = "bnicfvxvyblzzatvursw"

# PDF de pedido tem PII -> exige JWT no gateway; a função ainda valida staff por dentro.
[functions.generate-pdf]
verify_jwt = true

[functions.b2bwave-sync]
verify_jwt = false

# Notificações: gateway off; a função valida (usuário logado p/ evento, admin ou
# X-Cron-Secret p/ teste). Permite forçar um teste sem login admin.
[functions.notify-dispatch]
verify_jwt = false

# Auto-cadastro cria a ficha pendente logo após o signUp (ainda sem sessão) —
# gateway off; a função valida o auth user pelo email e só cria status 'pendente'.
[functions.register-customer]
verify_jwt = false