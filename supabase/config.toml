project_id = "bnicfvxvyblzzatvursw"

# PDF de pedido tem PII -> exige JWT no gateway; a função ainda valida staff por dentro.
[functions.generate-pdf]
verify_jwt = true

# `[functions.b2bwave-sync]` REMOVIDO em 02/set/2026 com a propria funcao.
# O bloco declarava `verify_jwt = false`: deixado aqui, uma funcao futura com esse
# nome nasceria PUBLICA por herdar esta linha, sem ninguem perceber.

# Notificações: gateway off; a função valida (usuário logado p/ evento, admin ou
# X-Cron-Secret p/ teste). Permite forçar um teste sem login admin.
[functions.notify-dispatch]
verify_jwt = false

# Auto-cadastro cria a ficha pendente logo após o signUp (ainda sem sessão) —
# gateway off; a função valida o auth user pelo email e só cria status 'pendente'.
[functions.register-customer]
verify_jwt = false

# Chamada pelo cron diario (net.http_post manda apikey + x-cron-secret, SEM
# Authorization). Com o default verify_jwt=true a requisicao morria no gateway
# ANTES de entrar na funcao: o job nao rodava e nem log ficava, porque o log e
# gravado dentro dela. A propria funcao autoriza (cron-secret ou admin/manager).
[functions.sync-container-eta]
verify_jwt = false
